import React from 'react';
import BackendAlerts from '../components/BackendAlerts';

/**
 * Alerts page component for the Backend module
 * Acts as a container for the alerts components
 */
export const AlertsPage: React.FC = () => {
  return <BackendAlerts />;
};