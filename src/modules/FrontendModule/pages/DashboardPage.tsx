import React from 'react';
import FrontendDashboard from '../components/FrontendDashboard';

/**
 * Dashboard page component for the Frontend module
 * Acts as a container for the dashboard components
 */
export const DashboardPage: React.FC = () => {
  return <FrontendDashboard />;
};