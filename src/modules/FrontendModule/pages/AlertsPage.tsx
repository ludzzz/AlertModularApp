import React from 'react';
import FrontendAlerts from '../components/FrontendAlerts';

/**
 * Alerts page component for the Frontend module
 * Acts as a container for the alerts components
 */
export const AlertsPage: React.FC = () => {
  return <FrontendAlerts />;
};